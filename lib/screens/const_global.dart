import 'package:flutter/material.dart';

const styleFiliere = TextStyle(fontSize: 50.0,);

const paddingButton = EdgeInsets.all(20.0);

const marginContainer = EdgeInsets.all(10.0);

const styleMatiere = TextStyle(fontSize: 30.0,);

const paddingPage = EdgeInsets.all(8.0);

const styleChapitre = TextStyle(fontSize: 20.0,);

const marginChapitre = EdgeInsets.all(3.0);